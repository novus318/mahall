/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['upload.wikimedia.org','localhost','server.nizamudheen.com'],
    },
  };

export default nextConfig;
